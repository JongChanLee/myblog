<header class="d-flex flex-row justify-content-between align-items-center p-3">
  <h1>
    <%= link_to 'TuTanKhamen.me', root_path %>
  </h1>

  <nav class="d-flex flex-row justify-content-around align-items-center">
    <h3 class="sr-only">menu</h3>
    <ul class="d-flex flex-row justify-content-around align-items-center">
      <li class="px-2"><%= link_to 'Articles', posts_path %></li>
      <% if user_signed_in? %>
          <li class="px-2"><%= link_to 'Log Out', destroy_user_session_path, method: :delete, data: {confirm: '로그아웃 하시겠습니까?'} %></li>
      <% else %>
          <li class="px-2"><%= link_to 'Log In', new_user_session_path %></li>
          <li class="px-2"><%= link_to 'Sign Up', new_user_registration_path %></li>
      <% end %>
    </ul>
  </nav>
</header>